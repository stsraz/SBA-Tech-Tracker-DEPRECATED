/**
 * @author Joseph Rasmussen
 */


var startup = function() {
	// Sets the initial load conditions
	var init = function() {
		// Check for user in xml file, in not, add user to xml file with jQuery dialog box
		// Set the jQuery UI elements
		// Check for user store information, display on dashboard
		// Check user time record information, display on dashboard
		// Use time record information to display analytics
	};
};